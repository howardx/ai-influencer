// Telegram surface: approval cards (✅/❌ first, ✏️/🔄 later), log channel,
// commands (/pause /resume /status /arc /commercial /photo).
// Lands with HEX-24 (minimal slice) and HEX-27 (full queue UX).
export {}
