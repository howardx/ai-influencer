// Metrics: daily_snapshot / post_metrics / queue_stats collection, weekly
// Telegram digest, monthly commercial-reach report (the case-study dataset,
// spec §10). Lands with HEX-30. action_costs recording already exists in
// storage/db.ts so every billable call is metered from day one.
export {}
